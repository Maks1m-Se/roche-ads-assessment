# Copy this file to .env and fill in your key. .env is gitignored and must never
# be committed; this example file carries no secret and is tracked on purpose.
OPENAI_API_KEY=your-key-here

# Optional. Defaults to gpt-4o-mini when unset.
# OPENAI_MODEL=gpt-4o-mini
